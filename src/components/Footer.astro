---
import Hr from "./Hr.astro";
import Socials from "./Socials.astro";


const currentYear = new Date().getFullYear();

export interface Props {
  noMarginTop?: boolean;
}

const { noMarginTop = false } = Astro.props;
---

<footer class={`${noMarginTop ? "" : "mt-auto"}`}>
  <Hr noPadding />
  <div class="footer-wrapper">
    <!-- TODO: Sponsor buttons here -->
    <Socials centered />
    <div class="footer-links">
      <span><a href="#">Credits</a></span>
      <span class="separator">&nbsp;|&nbsp;</span>
      <span><a href="#">View source</a></span>
      <span class="separator">&nbsp;|&nbsp;</span>
      <span><a href="#">Report a bug</a></span>
    </div>
    <p>
      No cookies, no ads, no tracking ― just blog
    </p>
    <div class="copyright-wrapper">
      <span>&copy; {currentYear}</span>
      &nbsp;
      <span>Rohit Mehta</span>
    </div>
    
  </div>
</footer>

<style>
  footer {
    @apply w-full;
  }
  .footer-wrapper {
    @apply flex flex-col items-center justify-between pt-6 /* min-h-44 /*py-6 /* sm:flex-row-reverse sm:py-4 */;
  }
  .link-button {
    @apply my-1 p-2 hover:rotate-6;
  }
  .link-button svg {
    @apply scale-125;
  }
  .footer-links a {
    @apply inline-block /*text-skin-accent*/ underline decoration-dashed underline-offset-4 hover:no-underline hover:underline-offset-0;
  }
  .copyright-wrapper {
    @apply my-2 flex flex-row items-center whitespace-nowrap /* sm:flex-row */;
  }
</style>
